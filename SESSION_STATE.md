# SESSION_STATE — Deckoala

> Read this file FIRST at the start of every session. The Resume Pointer below is the single next action.

## ▶ Resume Pointer
Cowork: audit **BRIEF-0014 — research library (PDF/text → AI source context) + PDF figure extraction into slides (image picker + MCP)** (implemented + committed + pushed). BRIEF-0013 (Gemini provider) landed just before it.

**One acceptance item is NOT evidenced and must be audited as such:** the authenticated browser walkthrough of `/app/research` was **not performed**. The implementation tool could not sign in — `root`'s password was changed in an earlier session, and its operating rules forbid it from creating an account or entering credentials. The HTTP surface behind that UI is covered end-to-end by integration tests against a **real PDF fixture** (`backend/tests/fixtures/paper.pdf`), including figure extraction, serving, attach-to-deck and delete. **The user must either do the UI pass themselves or supply a way in** before this brief can be called PASS on the "it runs in the UI" criterion (CLAUDE.md Rule 5).

The approved roadmap (BRIEF-0000…0012 + 0009b/c/d) is complete; 0013/0014 were added later from direct user requests. PWA/offline and realtime collaboration remain **pending user choice** (never approved). Deferred hardening (rate-limiting/lockout, register-path enumeration from BRIEF-0001) is still open.

Note: the combined "themes + docs + images" ask was **split** — 0009c = themes + custom CSS + safe frontmatter writer; 0009d = the in-web usage manual + image picker.

Repo is now on GitHub: `https://github.com/VoraprotSeesod/deckoala.git` (private) — push `main` after each brief lands.

---

## 1. Project snapshot
- **What:** Deckoala — self-hosted Markdown/LaTeX presentation web app (live preview, present in browser, PDF export, custom fonts, share links) at `deckoala.dimenshade.com`
- **Who/for:** the user + people they share the instance with (multi-user)
- **Replaces:** ad-hoc slide tools; must be trivially self-hostable (one compose.yml)
- **Language convention:** discussion/analysis docs in Thai; briefs, code, identifiers, UI copy in English (Thai UI i18n on roadmap)

## 2. Roles & workflow
- **Cowork** = design, briefs, audit. **Implementation tool** = code. **Serial only** (see CLAUDE.md §0).
- Loop: Cowork brief → user `ลุย <task>` → impl builds → user "done" → Cowork audit (PASS→next / FAIL→FIX prompt).

## 3. Architecture decisions (summary)
- [ADR-0001](decisions/ADR-0001-stack.md): SvelteKit static SPA + Rust/Axum + SQLite, single container `[USER]`
- [ADR-0002](decisions/ADR-0002-slide-engine.md): Marp Core rendered client-side; deck format = standard Marp Markdown
- [ADR-0003](decisions/ADR-0003-pdf-export.md): PDF via headless Chromium in-container, driven by chromiumoxide
- Full design: [docs/design/ARCHITECTURE.md](docs/design/ARCHITECTURE.md) · Requirements: [REQ-ANALYSIS-v1](docs/requirements/analyzed/REQ-ANALYSIS-v1.md)
- Brand: bg `#F8F8FF`, ink `#0B1215`; logo `assets/brand/logo.svg` (selection log: [docs/design/brand/logo-candidates.md](docs/design/brand/logo-candidates.md))

## 4. Briefs / task queue
| Command | Brief | Status |
|---|---|---|
| `ลุย BRIEF-0000` | infra & scaffolding | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0001` | auth & users | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0002` | deck CRUD + dashboard | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0003` | editor + live preview + autosave + revisions | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0004` | slide rail reorder + image upload | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0005` | present mode + speaker notes + presenter view | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0006` | PDF export via headless Chromium | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0007` | font manager (upload + Google Fonts) | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0008` | sharing (view/edit share links) | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0009` | polish: Thai UI i18n + app dark mode | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0010` | admin settings + root bootstrap + AI slide generation | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0011` | MCP server (`/mcp` + per-user tokens + stdio bridge) | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0009b` | command palette + keyboard shortcuts | **done** (2026-07-22, pr-review PASS) |
| `ลุย BRIEF-0009c` | theme gallery + per-deck custom CSS + safe frontmatter writer | **done** (2026-07-22, pr-review PASS) |
| `ลุย BRIEF-0009d` | in-app usage manual + easier image insertion | **done** (2026-07-22, pr-review PASS) |
| `ลุย BRIEF-0012` | visual editor phase 2: formatting toolbar + block inserter | **done** (2026-07-22, pr-review PASS) |
| `ลุย BRIEF-0013` | Gemini as a third AI provider | **done** (2026-07-25, pr-review PASS) |
| `ลุย BRIEF-0014` | research library + PDF figure extraction (image picker + MCP) | **done** (2026-07-25) — UI walkthrough **not evidenced**, see Resume Pointer |
| — | **Roadmap complete.** PWA / realtime collab remain pending-user-choice (not approved). | — |

## 5. Progress log
### 2026-07-21
- Requirements captured from user brief (Thai) → REQ-ANALYSIS-v1; discussion round 1 closed Q1–Q4 (framework SvelteKit, backend Rust, drag & drop phase-1 simple, multi-user + share links).
- ADR-0001..0003 accepted; ARCHITECTURE.md written; CLAUDE.md + skeleton laid down.
- Logo designed (4 candidates → 3-judge panel → `ears-monogram` won); saved to `assets/brand/` + visual check passed at 16–256px.
- BRIEF-0000 written and registered; adversarial doc-review (3 lenses, 12 findings) applied: scoped port/font invariants, added `db` to health gate, volume chown, `DECKOALA_STATIC_DIR`, reserved prefixes `/api|/assets|/fonts`, roadmap +BRIEF-0010 visual editor, presenter view → BRIEF-0005, PWA/theme gallery moved back to pending-user-choice.
- **BRIEF-0000 implemented** (same session, user said `ลุย`): SvelteKit SPA + Axum server + SQLite migrations (reversible pair) + Docker (multi-stage, chromium, non-root, healthcheck) + compose. Evidence: `cargo test` 2 passed / clippy `-D warnings` / fmt clean (Docker verify stage); `svelte-check` 0 errors; compose up → container healthy; `/api/health` = `{"status":"ok","db":"ok","chromium":true}`; landing verified in browser desktop + 375px mobile; teardown leaves only the volume. Rust pinned 1.88.0 (deps' MSRV forced bump from 1.84; Cargo.lock committed, builds `--locked`).
- Note for next briefs: down-migrations exist from 0001 but executing them needs sqlx-cli — bring it in with BRIEF-0001.
- **BRIEF-0001 implemented** (same session): register/login/logout/me + /api/instance; argon2id (pinned m=19456,t=2,p=1, spawn_blocking); tower-sessions SqliteStore (Lax, 30d, cycle_id on login, hourly expired-session cleanup); origin-check CSRF (authority vs Host or DECKOALA_PUBLIC_URL, null→403); atomic first-user-admin INSERT; login: uniform 401 + dummy-hash timing + 128-byte cap; SPA: /login 3-mode form, guarded /app shell, +error page. Two adversarial review rounds (design 11 findings → brief tightened pre-code; implementation 5 findings → all fixed, incl. Vite changeOrigin:false and logout-error surfacing). Evidence: 19 tests passed (Docker verify), svelte-check 0 errors, compose healthy, curl flow 201→200→204→401/403, browser UI flow desktop + 375px mobile (measured no h-scroll; pane input broke mid-run → clicks dispatched via DOM, same app handlers). New env: DECKOALA_ALLOW_SIGNUP / DECKOALA_SECURE_COOKIE / DECKOALA_PUBLIC_URL.
- Deferred to hardening brief (recorded in BRIEF-0001): rate limiting/lockout, register-path username enumeration.
- **BRIEF-0002 implemented** (same session): migration 0003 decks (+ `foreign_keys(true)` now enforced); AuthUser extractor; CRUD list/create/get/PATCH/soft-delete/duplicate/export with owner-scoping-as-404 on reads AND writes; default Marp template; export Content-Disposition (remove-filter ASCII + RFC5987 filename*); fixed-width `now_rfc3339` (lexicographic ordering); DefaultBodyLimit 4MB; dashboard (grid, new/import/rename/duplicate/export/delete, empty state) + read-only deck page. Two review rounds again (brief: 9 findings pre-code; code: 5 nits — all fixed incl. surrogate-pair-safe filename truncation and 401→/login on cached-layout page loads). Evidence: 37 tests passed (9 unit + 11 auth + 14 decks + 3 health), svelte-check 0/0, compose healthy, curl CRUD flow verified, browser flow desktop+375px incl. import-via-DataTransfer (title from filename), teardown clean earlier this session.
- Incident note: a tool-call edit containing backslash-u unicode escapes got decoded into literal control bytes inside +page.svelte (ripgrep then treated the file as binary) - rewrote the file cleanly; lesson: avoid backslash-u escape sequences in tool-call payloads, prefer codePoint filters in JS.
- **BRIEF-0003 implemented** (same session): migration 0004 revisions; transactional PATCH snapshot (BEGIN IMMEDIATE via sqlx begin_with → auto-rollback on drop, 300s throttle, stale-baseUpdatedAt forces snapshot for the clobber case, retention cap 50 with rowid tie-break); restore/list/get routes (owner+deck scoped, cross-deck revId 404); CSP header (blocks external egress from decks); CodeMirror 6 + marp-core client-side preview in Shadow DOM via **constructable stylesheets** (CSS never HTML-parsed → style-directive XSS breakout impossible), deckoala theme (#F8F8FF/#0B1215 + Noto Sans Thai), KaTeX with locally-bundled fonts (katexFontPath=/katex-fonts/ + document-level katex.min.css import — zero external requests); autosave state machine (debounce, retry, epoch guards, beforeNavigate flush for SPA nav, in-flight-save awaited before restore); revisions panel with view/restore. Two review rounds (brief: 8 findings folded pre-code incl. BEGIN IMMEDIATE, stale-base snapshot, CSP, sizeBytes-as-blob, cross-deck test; code: 6 findings fixed incl. the blocker SPA-nav data loss and the </style> XSS breakout → constructable stylesheets). Evidence: 49 tests passed (Docker verify), svelte-check 0/0, compose healthy, browser: live Thai+KaTeX preview / autosave persists across reload / revision view+restore / CSP blocks external img / XSS probe did not fire / desktop split + mobile tabs.
- launch.json written (.claude/launch.json: compose 8321, vite 5173, cargo 8080) — none started per user (compose stack already up).
- Deviation log (BRIEF-0003): none — KaTeX stayed on the primary (local-font) path; MathJax fallback not needed.
- **BRIEF-0006 implemented** (same session): PDF export via headless Chromium (ADR-0003). Deps added: chromiumoxide 0.7 (default-features=false + tokio-runtime), hmac/sha2/rand. NO migration (stateless HMAC print tokens; in-memory `print_secret`). `POST /api/decks/{id}/export/pdf` (session, owner-check before any Chromium): sha256(markdown+theme) cache under /data/exports (atomic temp+rename write), else acquire semaphore permit (2) → mint token → `render_pdf`: launch Chromium (`--no-sandbox --disable-gpu --disable-dev-shm-usage --host-resolver-rules` loopback-only SSRF enforcement) → CDP set `deckoala_print` cookie → navigate `/print/{id}` on `local_addr` (from DECKOALA_BIND) → poll `window.__DECKOALA_PRINT_READY` → printToPDF (prefer_css_page_size, 0 margins) → stream `application/pdf` with `.pdf` filename (shared `content_disposition(title, ext)` factored to lib.rs). RAII `BrowserGuard` tears the browser down on every path (whole render wrapped in `timeout(30s)`; close-before-abort ordering). `GET /api/print/{id}` (print-cookie only) + `/assets` serve extended to accept a matching print cookie (so Chromium loads deck images) — serve auth unified to session-owner OR print-cookie, unauthorized now 404 (was 401). Frontend: `/print/[id]` (token-fetch, all slides as full-bleed pages sized to viewBox, fonts+images awaited via tick()+decode → ready flag), PDF export buttons on editor + dashboard. Three review rounds (brief: 8 findings folded incl. Chromium-leak-DoS, token-TTL-in-queue, image-readiness, port-from-BIND, SSRF-at-browser-layer; code: 5 fixed incl. non-atomic cache write, BrowserGuard abort-before-close hang/leak, launch-outside-timeout permit pin, tick() vs microtask, safe_segment on export id). Evidence: 70 backend tests (Docker verify, NO Chromium there — token/print-cookie/asset-auth/owner units), vitest 16, svelte-check 0/0, fmt/clippy clean; **real Chromium in runtime container: export → application/pdf, %PDF-, image XObject + fonts embedded (Thai + KaTeX), ~2.8s; cache hit ~16ms identical bytes; owner 404; filename PDF Test.pdf.**
- New env note: DECKOALA_BIND port is honored by PDF export (Chromium dials 127.0.0.1:<that port>).
- **BRIEF-0004 implemented** (same session): migration 0005 assets; multipart upload (`POST /api/decks/{id}/assets`, magic-number sniff PNG/JPEG/GIF/WebP incl. RIFF+WEBP fourcc, 5MB cap, 8MB per-route body limit override verified, owner-scoped 404, safe_segment on deck_id + filename) + owner-scoped serve (`GET /assets/{deck}/{file}` on its own cloned SessionManagerLayer outside /api, nosniff + immutable cache). Frontend: `slides.ts` deriving slide boundaries from Marp's own `marpit_slide_open` tokens (setext/comment/indented-`---` never split → thumbnail↔reorder indices can't desync), CRLF-preserving reorder; slide rail with per-thumbnail shadow roots adopting the shared marpSheet (styled thumbnails, no re-render), native DnD reorder through the editor state path (not setEditorContent); image drop+paste (capture-phase) → upload → insert `![alt](/assets/…)` with markdown-escaped alt. katex CSS import moved to the editor so marp.ts is node-importable by vitest. Three review rounds (brief: 1 blocker [slide-split desync] + 6 fixed pre-code; code: 3 fixed incl. upload-race-on-deck-switch guard + multi-image partial-failure sync + upload deck_id safe_segment). Evidence: 61 backend tests + 16 vitest passed (Docker verify + native), svelte-check 0/0, compose healthy; browser: rail 3 thumbnails render, DnD reorder persists across reload, image drop/paste uploads + renders same-origin, 4.7MB upload proves 8MB override, alt-injection stripped, mobile 375px no h-scroll. New: vitest (`npm run test:unit`) is a native frontend gate.
- Deferred (BRIEF-0004, noted in brief): asset GC/orphan cleanup, storage quota, media library panel, touch-drag reorder on mobile.
- **BRIEF-0005 implemented** (same session, frontend-only — no backend change): `renderDeck` now returns per-slide `notes` from marp `comments` (directive comments like `<!-- _class -->` are auto-excluded, verified). New route `/present/[id]` (own owner-scoped load, 401→login / 404→error): audience mode (one slide letterboxed on ink via the SVG's own viewBox+preserveAspectRatio, keyboard ←/→/Space/PageUp/Dn/Home/End/n/p, `f` fullscreen, touch swipe, idle-auto-hiding control bar) and presenter mode `?presenter` (current + next slide in separate shadow hosts sharing the marpSheet, speaker notes as text, mm:ss timer with pause/reset, prev/next/Exit). Cross-window sync via `BroadcastChannel('deckoala-present-<id>')` with a hello/state handshake (fixes presenter-opens-on-wrong-slide) + hash seed; nav in either window moves both. Present entry points added to editor top bar + dashboard cards. Fixes from 2 review rounds (design: 6 findings incl. 2-surface presenter render, empty-deck guard, handshake, Esc-vs-fullscreen race, aspect-ratio letterbox; code: 5 incl. hash-seed-vs-handshake, `:hover` bar unusable on touch → JS idle timer, focused-button swallowing keyboard nav → blur, presenter Exit button + window.close fallback, popup-blocked notice). Also fixed a render bug found in-browser: extracting a lone `<svg>` dropped the `.marpit` wrapper the theme scopes to → slides rendered transparent; now wrapped (applied to the editor rail too). Evidence: svelte-check 0/0, vitest 16, build ✓, backend suite unchanged (61 tests green from BRIEF-0004 verify); browser: slide renders letterboxed + KaTeX, arrow/swipe nav + counter, presenter shows current/next/note/timer, handshake synced presenter to the audience's slide with NO hash, bidirectional sync, **note shown in presenter only (not on slide)**, mobile swipe no h-scroll.
- **BRIEF-0010 implemented** (same session — NEW user requirement: AI + MCP + admin settings + default admin account): migration 0008 `settings` (key/value). **Root bootstrap**: `auth::seed_root` seeds `root` (is_admin=1) with `DECKOALA_ROOT_PASSWORD` or the built-in `Admin123456@`, called **only from `main.rs`** — deliberately NOT `init_db()`, because every integration test calls `init_db` and relies on "first signup is admin" (seeding there would have broken the whole suite). Idempotent single-statement insert; the `root_password_is_default` flag is set **only** when the built-in default was used, and cleared on change. New `POST /api/auth/password` (verifies current, argon2 reuse, rotates the session; other sessions survive — deferred hardening, tower-sessions has no per-user enumeration). `/api/auth/me` gained `rootPasswordIsDefault` + `aiEnabled` via a separate `MeDto` so register/login shapes are unchanged, and both flags are **session-gated — never on the anonymous `/api/instance`** (tested: key absent, not false). **Admin settings** (`settings.rs`, AdminUser-gated): GET returns provider/baseUrl/model + `apiKeySet`/`apiKeyLast4` but **never the key**; PUT keeps the stored key when omitted, has an explicit `removeApiKey`, and refuses to enable without base URL + model + key. **AI** (`ai.rs`): Anthropic + OpenAI-compatible adapters (local Ollama/LM Studio supported), `join_url` never doubles `/v1`, per-user throttle + global semaphore + prompt/context/response caps + `max_tokens` + timeout + `redirect::Policy::none()`, provider errors never echoed (key-leak safe), fenced output stripped. Frontend: `/app/admin` page (TH/EN, dark-clean) with the default-password warning + change-password + AI form (key field write-only), and an **AI button + dialog in the editor gated by an `aiEnabled` prop set ONLY by the owner route** — never on `/s/[token]`. **CLAUDE.md §2 amended**: exactly two server-side outbound calls (Google Fonts + the admin-configured LLM); viewer pages still zero. Env plumbed through `compose.yml` + `.env.example` + README (compose's `environment:` is an allow-list, so a new var would otherwise be silently dropped; blank is treated as unset). Pre-code adversarial brief-review (56 agents) folded **13 findings**. Evidence: fmt + clippy `-D warnings` clean, **cargo test all green** (lib 25, **admin 9/9**, shares 8, decks 14, auth 11, fonts 4, assets 4, export 4, revisions 11 — 0 failures); svelte-check **0/0**, vitest 22. **Runtime on a wiped volume**: startup WARNs about the default; `root`/`Admin123456@` signs in; anonymous `/api/instance` has no flag; `/me` shows `rootPasswordIsDefault:true, aiEnabled:false`; AI generate → **503** while unconfigured; storing a key never returns it (`••••9876`); change-password → wrong 401 / ok 204 / **flag cleared** / old password 401, new 200; AI button + Thai dialog appear for the owner, **absent on an anonymous share-edit link where a direct `/api/ai/generate` returns 401**; zero external requests on every page.
- **BRIEF-0009 implemented** (same session, frontend-only — no backend change): **Thai UI i18n** (default Thai + remembered EN toggle) + **app dark mode** (remembered, OS-default). i18n: `$lib/messages.ts` = pure catalog (th+en) + `translate()` with a total fallback chain (locale→en→key) and `{name}` interpolation, message values may be functions for pluralization ("1 slide"/"N สไลด์") — kept plain-TS so vitest imports it (no rune-transform in the test config); `$lib/i18n.svelte.ts` = reactive `locale`/`theme` as `$state` on a singleton class (not a reassigned export), `t()`/`formatDate()`/`formatTime()` follow the toggle, all `localStorage`/`matchMedia`/`document` reads `browser`-guarded. `<SettingsToggle>` (lang+theme) in every header; every page/component migrated to `t()` + locale-aware dates. Dark mode: `app.css` promotes the palette to semantic vars (`--dk-surface`, `--dk-danger/success/warn*`) with light `:root` + dark `:root[data-theme=dark]` (AA-adjusted); hardcoded `#fff`/status literals → vars, `color-mix(var(--dk-ink) …)` borders invert for free; **slide surfaces stay brand-light** (thumbnails pinned `#F8F8FF`, preview slides, present stage, /print). `app.html` pre-paint inline script (CSP-safe, no nonce) applies saved lang+theme before first paint (no flash); root `+layout` effect keeps `<html lang>`/`data-theme`/theme-color synced. Two review rounds: **brief-review (30 agents) folded 8** (plain-messages split for vitest, warn/success dark vars + AA, `browser` guards, `$state`-singleton export pattern, parent-injected string localization, pluralization + locale dates, keep CSP unsafe-inline); **code-review** folded (+ 1 self-caught regression: the `#fff`→var sweep darkened slide thumbnails → reverted `.thumb` to `#F8F8FF`). Evidence: svelte-check **0/0**, vitest **22** (slides + new messages suite), build ✓; backend unchanged (cargo suite green from BRIEF-0008). **Runtime:** fresh visitor defaults to Thai; toggle flips every string EN↔TH and back; dark toggle darkens chrome (body `#0f141a`, editor pane `#1b232c`) while the **slide preview + thumbnails measure `#F8F8FF`** (light-branded); both prefs persist across reload/nav; **zero external requests**; Thai renders via bundled Noto Sans Thai. Known limitation (accepted): backend `ApiError` text stays English (frontend-only scope). Nice-to-haves sequenced into follow-up briefs (0009b command palette, 0009c theme gallery).
- **BRIEF-0008 implemented** (same session): migration 0007 `share_links` (`id, deck_id, token UNIQUE, permission view|edit, created_at, expires_at, revoked_at`); `shares.rs` — owner mint/list/revoke (owner-scoped; **revoke double-scoped by id AND deck_id**) + anonymous token routes `GET/PATCH /api/s/{token}` (PATCH edit-only), token revisions list/get/restore (edit), token assets upload (edit), `POST /api/s/{token}/export/pdf` + `GET /api/s/{token}/export` (view+edit). 256-bit hex tokens; unknown/revoked/expired → uniform **404** (no oracle). **Expiry canonicalized to UTC at mint** (RFC3339-with-offset required, naive→422) so the TEXT active-check can't fail open. **Reuse over fork:** decks/export/assets refactored to `pub(crate)` `*_core` fns returning `Response` (never the private rows — dodges `private_interfaces` under `-D warnings`); the revision-snapshot transaction stays single-source (token edit resolves the deck's real owner and delegates). Asset serve now also honours a **per-deck share cookie** (`deckoala_share_{deckId}`, `Path=/assets/{deckId}`, HttpOnly, Secure-from-config), re-resolved to an active share every request → revocation immediate. Anonymous exports use a **separate tighter semaphore (permits=1)** so a leaked edit link can't starve owner exports; `Referrer-Policy: no-referrer` (token is a URL bearer). Frontend: editor extracted to `$lib/components/DeckEditor.svelte` (driven by an `EditorAdapter`), reused by `/app/deck/[id]` (owner) + `/s/[token]` (edit); `/s/[token]` view = read-only render + Present + PDF/.md; `ShareManager` modal (create/copy/revoke + status) on editor + dashboard cards; `SharePresent` audience fullscreen overlay. Two adversarial review rounds: **brief-review (30 agents) folded 4 confirmed** (expiry canonicalization, token-aware present, token .md export, separate export semaphore) + 5 correctness notes; **code-review (token-authz/refactor/frontend)** — token-authz & refactor lenses clean, **1 low frontend finding folded** (SharePresent didn't focus itself → keyboard nav no-op behind the covered Present button; now focuses `.audience` on mount). Evidence: backend fmt/clippy `-D warnings` clean, `cargo test` all green (**shares 8/8** — owner-only mgmt + cross-tenant revoke blocked + view-reads/edit-writes+snapshot + cross-deck 404 + expiry fail-closed + deleted→404 + share-cookie asset auth + migration v6 roundtrip; lib 21), svelte-check **0/0**, vitest 16, build ✓; **runtime, logged OUT (anonymous):** view link renders 3 slides + Thai + image (share cookie), Present, PDF `%PDF-` (2.6s), .md; **view PATCH → 403**; edit link full editor **autosaves via token (revision snapshotted)** + Present (keyboard nav fixed) + PDF, no owner Share control; **revoke → immediate 404 while the other link keeps working**; **zero external requests** on every shared page; owner Share modal shows active/revoked links with copy/revoke.
- **BRIEF-0007 implemented** (same session): migration 0006 `fonts` keyed `(family, weight, style, unicode_range)` so multi-subset Google families (Thai etc.) keep every unicode-range slice; `AdminUser` extractor (reuses AuthUser → 401 without session, then `is_admin` → 403). `fonts.rs`: `upload` (AdminUser, magic-byte sniff wOF2/wOFF/OTTO/ttf, family/weight/style validation, 5MB + 8MB-route caps, 201), `google` (AdminUser, self-built css2 URL from validated inputs, `redirect::Policy::none()`, exact `fonts.gstatic.com` host check via parsed URL, size caps + 10s timeout, ONE transaction), `list` (AuthUser), `delete` (AdminUser, unlinks file only when no other row references it), public `/api/fonts.css` (no-cache, css_escape family) and session-less `/fonts/{filename}` serve (safe_segment, nosniff, immutable) so Chromium/viewers load fonts. Frontend: `/app/fonts` manager (upload + Google install + delete, admin-only) + document-level `<link>` to `/api/fonts.css` in app.html. Deck font picker DESCOPED (frontmatter-writer corruption risk) — users set a font via a Marp `style` directive (page hint shows the snippet). Adversarial 4-lens Workflow review (ssrf/authz/injection/correctness, 14 agents, per-finding verification): **3 findings folded** — (1) `read_capped` treated a mid-stream error as clean EOF → truncated font/css could be stored as "installed"; now returns None on stream error; (2) `google()` left orphan files under /data/fonts when an INSERT hit `ON CONFLICT DO NOTHING` (re-install / add-a-weight); redundant download now deleted, only inserted rows tracked for rollback; (3) defense-in-depth: Google-parsed style/unicode-range clamped/stripped + filename/format css-escaped before reaching fonts.css. Injection/authz findings verified as refuted (server-minted or MITM-only). Evidence: Docker verify green — fmt/clippy `-D warnings` clean, **lib 19 + fonts 4 + all suites passed, exit 0** (incl. migration undo→v5 roundtrip + unicode_range sanitizer unit test); image rebuilt, compose healthy (`/api/health` ok, only 8321 published); **runtime browser: admin upload → 201 in list + fonts.css; Google "Sarabun" [400,700] → 8 subset variants local; browser issued ZERO external requests (all 40 → localhost, no gstatic/googleapis); PDF export subset-embeds `AAAAAA+Sarabun-Bold`/`Sarabun-Regular` (Thai+Latin); orphan-fix confirmed — two full re-installs kept files==rows (9==9), add-weight 600 → 13==13.**

## 6. Open questions / blockers
- Q5 fonts: instance-level admin-managed — shipped that way in BRIEF-0007; no objection raised, considered settled.
- Q6 speaker notes: yes via Marp comments (`[STD]`); presenter view = roadmap.
- Q7 UI language: RESOLVED — Thai i18n shipped in BRIEF-0009, defaulting to Thai with a remembered EN toggle (user's explicit choice).

## 7. Pending tasks for the implementation tool
- None — next step is design-side: Cowork writes BRIEF-0009 (polish: Thai UI i18n + nice-to-haves), then the user hands off with `ลุย BRIEF-0009`.
- Follow-up (deferred from BRIEF-0007): a safe YAML-frontmatter writer so a deck can pick an installed font from the UI (currently done by hand via a Marp `style` directive).
- Follow-ups (deferred from BRIEF-0008, noted in the brief): per-user collaborator invites / real-time multi-cursor; public deck discovery, comments, view analytics; in-place token rotation; email delivery of links.
- **BRIEF-0011 implemented** (2026-07-21, same session — the second half of the user's AI requirement: "ต้องมี mcp ให้เชื่อมด้วย เผื่อจะใช้ AI นอกโปรแกรมเชื่อม"): migration 0009 `api_tokens` (`id, user_id FK, name, token_hash UNIQUE, created_at, last_used_at, revoked_at`, index on user_id). **Tokens** (`tokens.rs`): value = `dko_` + 256 random bits hex, stored **only as SHA-256** (a fast hash is correct for a high-entropy secret; argon2 is for passwords), plaintext returned by exactly ONE response ever; owner CRUD is **session-gated** so a token can never mint another; revoke **double-scoped by id AND user_id**; name validated + 20 active tokens/user cap. `McpUser` extractor resolves `Authorization: Bearer` → user_id with a best-effort `last_used_at` touch, and deliberately emits **no `WWW-Authenticate` header** (it pushes MCP clients into OAuth discovery, whose `/.well-known/*` probes would hit the SPA fallback and get HTML 200 — so `/.well-known/*` now returns 404 too). **MCP** (`mcp.rs`): hand-rolled JSON-RPC 2.0 at `POST /mcp`, mounted **outside `/api`** in its own state-carrying router with its own `DefaultBodyLimit::max(4MB)` and **no CORS layer** (a browser cannot set `Authorization` cross-origin, so there is nothing to allow and adding one would weaken `/api`); `initialize` (echoes a known protocolVersion), `ping`, `tools/list`, `tools/call`; batch arrays supported; a message with **no `id`** is a notification → HTTP 202 empty body; `-32700`/`-32601`/`-32602`; tool failures travel as `isError` content, transport auth failures stay HTTP 401. **Four tools scoped to the TOKEN's user** — `list_decks`, `get_deck`, `create_deck`, `update_deck`; **no delete** (destructive over a long-lived token), and deliberately NOT copying `shares.rs`'s owner-resolution (that resolves a deck's real owner, which would break owner scoping). **decks.rs refactor** (the risk item): typed `DeckError` + data-returning helpers `list_decks_data`/`fetch_deck_data`/`create_deck_data`/`update_deck_data` with **validation moved INSIDE** them, so MCP cannot write rows the HTTP path would reject; `update_deck_data` keeps the single `BEGIN IMMEDIATE` revision-snapshot transaction, and the HTTP handlers now wrap the same helpers. Per-user **write budget** (60 writes/60s fixed window, reads unlimited) bounds a runaway agent loop. Frontend `/app/tokens` (TH/EN, dark-clean, responsive): create → value revealed **once** in a copyable box, list with created/last-used/status + revoke, endpoint URL, and **both** client snippets (HTTP+header for Claude Code; the stdio bridge for Claude Desktop, which cannot send a static bearer header). `tools/mcp-stdio-bridge.mjs` = dependency-free Node shim (stdin/stdout JSON-RPC ⇄ `POST /mcp`). MCP is **inbound only — no new outbound call**, so CLAUDE.md §2 is unchanged. Reviews: pre-code adversarial brief-review (48 agents) folded 15 findings; **post-code review (197 agents, 10 lenses × 3 refute-by-default skeptics) produced ZERO surviving lens findings**, and the completeness critic caught 3 real ones, all fixed: (1) **HIGH — agent-written Markdown silently lost the deckoala theme** (`create_deck_data` only templates when markdown is *absent*, so a model's frontmatter-less Markdown rendered in marp-core's stock theme and Thai text lost its Noto Sans Thai fallback in preview/present/PDF) → `ensure_front_matter()` backfills on create+update, never duplicates, and leaves an owner's custom frontmatter (e.g. a `style:` font override) verbatim; tool descriptions now state the contract like `ai.rs` does; (2) the "ready-to-paste" config snippets hardcoded `dko_…` while the real value was on screen and unrecoverable once dismissed → snippets now interpolate the live token, with a note saying so; (3) this progress log + Resume Pointer were unwritten. Also self-caught in runtime testing: the stdio bridge crashed on exit (`process.exit()` during teardown → libuv `UV_HANDLE_CLOSING` assertion on Windows) → drains naturally + handles EPIPE. Evidence: `cargo fmt --check` + `cargo clippy --all-targets -D warnings` clean, **cargo test 115 green** (lib 31, admin 9, assets 8, auth 11, decks 14, export 4, fonts 4, health 4, **mcp 11**, revisions 11, shares 8), svelte-check **0/0**, vitest 22. **Runtime (compose, healthy, only 8321 published):** minted a token in the UI (list contains the value 0 times) → `curl` drove a real handshake: `initialize` echoed 2025-06-18, `tools/list` returned the 4 tools with no delete, `create_deck` + `update_deck` succeeded and **the decks appeared in the web UI** with `revisions: 1`; notification → 202 + 0 bytes; batch → reply only for the request member; malformed → -32700; unknown tool → -32602; a 201-char title → `isError`; **401 with no token, a garbage token, and a revoked token** (revoke 204 → immediate 401); a second user's token got `not found` for another user's deck; `/.well-known/oauth-authorization-server` → 404; the **stdio bridge** ran under real Node and created a Thai-titled deck (exit 0); **zero external requests** on the page (all 80+ → localhost); light `#F8F8FF` + dark both clean, 375px no horizontal page scroll (snippets scroll inside their own box).
- Host note (2026-07-21): C: had filled to 0.1 GB free and Docker Desktop would not start (buildkit I/O error). With the user's approval Docker's 67.6 GB data disk was moved to `D:\DockerData\disk\docker_data.vhdx` with an NTFS junction left at `%LOCALAPPDATA%\Docker\wsl\disk`; unused build cache + untagged layers pruned. C: back to ~68 GB free; the `deckoala_deckoala-data` volume and all tagged images survived.

### 2026-07-22
- **BRIEF-0009b implemented** (command palette + keyboard shortcuts; **frontend only** — no migration, no endpoint, no backend change, no new dependency). Brief authored this session, then hardened by a **318-agent pre-code adversarial review** (8 lenses × 3 refute-by-default skeptics): all lens findings were refuted, and the **completeness critic produced 8**, all folded before a line of code was written — async deck-list arrival re-ordering rows under the user's finger (→ selection tracked by **command id**, updates buffered, `Enter` gated until the first fetch settles, **and destructive commands removed from the palette entirely** so a stray Enter can never delete a deck); gesture-gated commands silently dead (→ `run(ctx)` invoked **synchronously** before any `await`, because `fileInput.click()` needs transient user activation that an `await` consumes, and pages contribute their own handlers rather than the layout reconstructing state it cannot reach); a stale deck jump unmounting the whole app shell (→ new `routes/app/+error.svelte`, since `/app/deck/[id]`'s `error(404)` otherwise resolves to the ROOT boundary and takes the header + palette + global key listener with it); "Untitled deck" rows indistinguishable (→ two-line rows with `updatedAt`, newest first); a centred auto-focusing dialog sitting behind the phone keyboard (→ top-anchored sheet, `svh` sizing, body scroll lock); a header button wrapping the header and breaking `DeckEditor`'s `calc(100dvh - 8.5rem)` (→ icon-only under 720px); "degrade quietly" swallowing session expiry (→ 401/403 goes to `/login`, only network/5xx degrade); and the help sheet omitting present mode (→ one shared `SHORTCUTS` table covering app/editor/present, scope stated per group).
  Built: `$lib/commands.ts` (registry + code-point-safe subsequence scorer — must match **Thai** labels, so no `charCodeAt` ASCII assumptions) and `$lib/shortcuts.ts` (`SHORTCUTS` table + `match()`), both **pure** modules importing nothing from `$app/*` so vitest loads them by relative path like `messages.test.ts`; `$lib/palette.svelte.ts` (Svelte context handle: `register`/`open`/`invalidateDecks`); `CommandPalette.svelte` (combobox/listbox ARIA, focus trap + restore, top-anchored sheet); `ShortcutHelp.svelte`; `routes/app/+error.svelte`; `/app/+layout.svelte` wiring (overlays, window key listener, lazy `api.decks.list()` on first open — the layout load returns only `user`, so loading decks on every `/app` page would tax fonts/tokens/admin for nothing); dashboard + editor command contributions; `Prec.highest(keymap.of(...))` in DeckEditor for `Mod-S`/`Mod-K` (CodeMirror's own keymap beats any window listener; plain `Mod-k`/`Mod-s` are unbound but **`Shift-Mod-k` is `deleteLine`**, so `Mod-Shift-P` is the second palette chord); TH/EN copy.
  **Post-code review (241 agents, 9 lenses × 3 skeptics) found 5 real bugs the browser pass had missed, all fixed:** (1) **HIGH — `Mod-S` outside CodeMirror opened the browser's Save-page dialog**: `match()` returns `save` for the whole editor route but the layout's switch had no `case 'save'`, so with focus on the deck-title input, the slide rail or plain `<body>` (the default, since CodeMirror is never auto-focused) Ctrl+S reached the browser — my own test had only ever dispatched it inside CodeMirror, which is exactly why the keymap layer looked fine; (2) **HIGH — the dark-mode backdrop was near-WHITE**: `color-mix(in srgb, var(--dk-ink) 45%)` inverts because `--dk-ink` is `#e7ebef` in dark mode, washing the page out instead of dimming it → fixed brand-ink `rgba(11,18,21,0.45)` matching ShareManager/the AI dialog (box-shadows too); (3) **HIGH — holding `n` machine-gunned deck creation** (no `event.repeat` guard); (4) `Esc` was dead for the AI and Share dialogs and `n`/`?` fired behind them (`overlayOpen` knew only this brief's overlays → now counts any open `[role="dialog"]`, which those modals already render); (5) opening an overlay shifted the page by the scrollbar width (→ `scrollbar-gutter: stable`). Accepted as unfixable: `Mod-Shift-P` collides with Firefox's New Private Window, which page JS cannot intercept — `Mod-K` is the primary binding and works everywhere.
  Self-caught in runtime testing: **page-contributed commands shipped without English aliases**, so with the UI in its default Thai, typing "import" matched nothing against `นำเข้า .md` (base commands had `keywords`, page ones did not) → aliases on all six page commands + a regression test; and `isTypingTarget` now falls back to `document.activeElement` when an event carries no usable target (this app renders the preview and slide rail into **shadow roots**, which retarget events).
  Evidence: svelte-check **0 errors / 0 warnings**, vitest **60 passed** (34 new across `commands.test.ts` + `shortcuts.test.ts`), `npm run build` ✓, compose healthy (`/api/health` ok, only 8321 published); backend untouched, so its 115 tests stand from BRIEF-0011. **Runtime browser:** `Mod-K` opens the palette from the dashboard **and from inside CodeMirror** (`Prec.highest` fires, default prevented); typing a title + Enter navigates to that deck; deck rows are two-line and newest-first; **`Mod-S` saves with the browser dialog suppressed BOTH inside CodeMirror and with focus on the title input** ("Unsaved changes" → "Saved 2:21:39 AM"); palette → `Import .md` fires `fileInput.click()` (proving the synchronous-run rule); `?` shows the sheet with **`Ctrl`, not `⌘`**, including the present-mode group; typing `?`/`n` in the editor inserts the character; `Esc` closes and returns focus into CodeMirror; `n` on the dashboard → `POST /api/decks 201`; the language toggle re-labels the palette live (Thai ↔ English); the backdrop measures `rgba(11,18,21,0.45)` **identically in both themes**; mobile shows the icon-only button with a top-anchored sheet and body scroll locked; **zero external requests** (~200, all localhost). Known limitation: the browser pane will not shrink below 502px, so the 375px case is evidenced by the active `max-width:720px` rule and zero overflow at 502px rather than a true 375px viewport.
- **BRIEF-0009c implemented** (2026-07-22): theme gallery + per-deck custom CSS + the safe YAML frontmatter writer deferred since BRIEF-0007. The combined "themes + docs + images" ask was **split** (0009c cluster / 0009d docs+images) after a **253-agent pre-code review** (9 lenses × refute-by-default skeptics) — all lens findings refuted, the completeness critic produced 4 real ones, folded before coding. Two design facts reshaped it: (a) a **strict CSP already ships** (`default-src 'self'; img-src 'self' data: blob:`), so external `url()`/`@import`/`![](https://…)` are already blocked at the browser on every served page incl. `/print` — the custom-CSS sanitizer is therefore **defense-in-depth**, proven by UNIT test not a network check (which the CSP would pass anyway); (b) `slides.ts` already had `frontMatterEnd()` (marpit boundary) — the writer reuses it so detection can't diverge. Built in the mandated risk-order: **`frontmatter.ts`** (getScalar/setScalar/getBlock/setBlock/getStyleContent/removeDirective, reuses the marpit boundary; the writer OWNS indentation so no user CSS line — `---`, de-indented, `:` — can break out of the `style: |` block or read as a slide fence) → **`css-sanitize.ts`** (strips comments+line-continuations first, then `@import`, then neutralizes external `url()`/`image-set`/`@font-face src` in any property after decoding CSS escapes; allows `data:`/`blob:`/same-origin incl. `/katex-fonts/`) → wired **inside `renderDeck()`** as the single choke point so all six render surfaces (preview, rail, present, print, share view, SharePresent) + gallery thumbnails are covered by construction, operating on the user `style:` block only so KaTeX fonts survive → **three themes** (`deckoala`, `deckoala-dark`, `deckoala-bold`) all Thai-capable + zero-external, each shipping `.columns`/`.center` utility classes for per-slide layout via `<!-- _class -->` under html:false → **ThemeGallery** (live thumbnails render a frontmatter-swapped copy per theme in shadow roots; apply → `applyEditorContent()` then flush) → **CustomCssModal** (a `<textarea>`, not a second CodeMirror — no new dep, no keymap fight) → **backend theme-column sync** (`theme_from_markdown` in create + update_in_tx, so the stored column never disagrees with the render — the bug BRIEF-0011 flagged). Also folded a real latent bug in the shared `frontMatterEnd`: it closed frontmatter at an **indented** `---` (`.trim()`), which would corrupt any deck whose `style:` block contained a `---` line → anchored to column-0. Two shortcut/UX fixes rode along: `match()` now blocks ALL shortcuts except Esc behind an open overlay (Mod-K was opening the palette over a modal), and the palette Save tracks its promise so a flush-before-export awaits it. **Bundled the user's dark-mode-contrast fix** (separate 2026-07-22 request): `app.css` gained `color-scheme` on both roots + explicit `select`/`option`/`::placeholder` defaults + a `--dk-muted` token, because native `<select>` popups and placeholders were rendering dark-on-dark (measured after: dropdown 13.24:1, placeholder 6.47:1). **Post-code review (30 agents)** confirmed 1 finding (inline `style:` scalar bypassed the block-only sanitizer — verified against real marp-core) + 3 I hand-verified as real (setBlock YAML-indent corruption when the first CSS line is more-indented than a later one; shortcuts-over-modal; ThemeGallery Tab-trap), all fixed (+ `getStyleContent` covering both style forms; left-trim each block line to one indent). Evidence: `cargo fmt --check` + `clippy -D warnings` clean, **cargo test lib 35** (incl. 4 theme-parser tests) + all suites green; svelte-check **0/0**, vitest **98** (frontmatter 24, css-sanitize 12, catalog-parity 1, + existing), build ✓; stack healthy. **Runtime browser (fresh `verify9c` user — note: `root`'s password was changed by a prior session's test, so it's no longer `Admin123456@` on this dev volume):** theme gallery shows 3 Thai-labelled live thumbnails; applying `deckoala-dark` → server `theme:deckoala-dark` (column synced), frontmatter updated, **slide body byte-identical** (Thai + `$$E=mc^2$$` intact); custom CSS `h1{color:#ff3366}` applies (compiled to `rgb(255,51,102)`) in **both block AND inline `style:` forms** while an external `url(https://…)` is neutralized to `url("about:blank")` and `@import` stripped, **KaTeX math intact**; CSS modal dark-mode textarea 17.51:1; Esc closes + scroll-lock restored; **zero external requests** (an injected evil `url()`+`@import` fetched nothing).
- Known dev-env note: `root`/`Admin123456@` no longer valid on the persisted `deckoala-data` volume (changed in an earlier session's change-password test). Use a freshly-registered account for manual testing, or reset via the DB.
- **BRIEF-0009d implemented** (2026-07-22): the in-web **usage manual** + **easier image insertion** the user explicitly asked for ("อยากให้เอกสารวิธีใช้... จะให้คำอยู่ตรงกลาง จะให้ใส่รูป จะให้เลือก font แบบแต่ละหน้าไม่ซ้ำกัน อยากให้อยู่ในเว็บเลยเป็นคู่มือการใช้งาน"). **Backend (additive, no migration):** `GET /api/decks/{id}/assets` (owner-scoped via the parent deck — the assets table has no owner column — 404 for foreign/deleted) + `GET /api/s/{token}/assets` (edit-only: view→403, unknown→404), both added as `.get()` on the existing POST MethodRouter; `EditorAdapter.listAssets()` (owner + share). **Manual:** shared `guide-content.ts` (section title/intro message keys + LITERAL Marp snippets — never keyed, so they can't render as a raw key) rendered two ways by `GuideBody.svelte`: a browsable **`/app/guide`** route (nav + palette `nav.guide`, Copy only) and an editor **`SlideGuide.svelte`** modal (Copy + Insert-at-cursor). Covers exactly what the user named — centering (`<!-- _class: center -->`), images + sizing, and the **per-slide font recipe** (install on Fonts page → name a class in Custom CSS → `<!-- _class: thai -->`), plus columns/backgrounds/math/notes. **Image picker** `ImagePicker.svelte`: 🖼 top-bar button + `page.image` palette entry; **upload a new image OR reuse one already in the deck** (thumbnail grid from listAssets), Inline size (Small/Medium/Original) vs Background mode (bg/fit/left/right) — mutually exclusive because Marp's `w:` and `bg` don't combine; alt defaults to the sanitized filename. `slide-syntax.ts` = the pure size/mode→markdown mapping. Guide/picker/image inserts go through a new `insertAtCursor()` on the editor's dispatch path; all palette entries folded into DeckEditor's SINGLE register array. `docs/USER-GUIDE.md` + README link. **Post-code review (44 agents)** confirmed one real bug — **insert-at-offset-0 corrupted the frontmatter**: a freshly-loaded editor's caret is at 0 and the Guide/Image buttons fire before the user clicks in, so Insert prepended the snippet before the `---` fence → Marp stopped parsing frontmatter (theme lost, directives leaked as slide text), autosaved. Fixed with a `bodyStartOffset()` guard (in `slides.ts`, reuses the marpit boundary) applied to BOTH insertAtCursor and the drop/paste image path; +3 tests. Also folded three cheap real fixes: alt input hidden in Background mode (bg images carry no alt), the hardcoded English `'preview'` alt → decorative empty, and "Full width" relabelled "Original size"/"ขนาดจริง" (no width = natural size) with the center copy clarified to "horizontally". Evidence: `cargo fmt --check` + `clippy -D warnings` clean, **cargo test 122** (assets 11 incl. 3 new list tests: owner list, foreign 404, share edit-lists/view-403/unknown-404); svelte-check **0/0**, vitest **107** (slide-syntax 6, bodyStartOffset 3, + existing), build ✓; stack healthy. **Runtime browser (verify9c):** `/app/guide` shows 7 Thai sections (center/images/**per-slide fonts**/…), 12 Copy snippets, no Insert, **zero raw i18n keys**; image picker empty-state → upload (201) → reuse grid populates → click → configure → **`![my-photo.png w:640](/assets/…)` inserted** (alt = filename, Thai size options); slide-guide modal 12 Insert buttons, insert grows the doc + closes; guide `<pre>` dark-mode 11.95:1; **the corruption fix re-verified on a brand-new deck** — inserting the columns snippet WITHOUT focusing the editor lands it AFTER the `---` fence with `theme: deckoala` intact; **zero external requests**.
- **BRIEF-0012 implemented** (2026-07-22) — the roadmap's final item: **visual editor phase 2 = a formatting toolbar + block inserter** over the CodeMirror editor, so a user can build/format slides by clicking without writing Marp Markdown, while the deck stays standard Marp Markdown (ADR-0002, no lock-in — no WYSIWYG canvas, no parallel model). **Frontend only** (no migration/endpoint/dependency). Core is **`md-format.ts`** — pure, DOM-free, UTF-16-offset (same model as CodeMirror, NOT code points) selection transforms: `wrapInline` (bold/italic/strike/code, whitespace-trimmed wrap, exact-length unwrap so italic never eats a `*` off bold), `toggleLinePrefix` (h1/h2/h3 heading-exclusive, bullet/numbered/quote mutually exclusive via a family regex, numbered detected by `^\d+\. ` so real `2.`/`3.` toggle off, **skips `---` slide separators** and blank lines), `insertLink`, `insertBlock` (on its own lines, blank line before AND after). Shared block strings in **`slide-blocks.ts`** (the same `.columns`/`.center` classes 0009c/themes/guide use). **`EditorToolbar.svelte`** = ~19 `role=toolbar` buttons (text / structure / insert groups; headings are three buttons not a dropdown; Image reuses the existing picker). DeckEditor: **`applyTransform`/`insertBlockAt`** clamp the read selection to `bodyStartOffset` for EVERY transform (the pre-code BLOCKER: a click on a never-focused editor at selection {0,0} would otherwise prepend before the `---` fence and lose the theme), run through `applyingRemote`+`syncFromEditor` (autosave/snapshot/preview), and use a **minimal-diff dispatch** (common prefix/suffix preserved) so scroll + undo survive; **`Mod-B`/`Mod-I`** in the existing `Prec.highest` keymap (beats basicSetup's `Mod-i`=selectParentSyntax) calling the same guarded path; a **toolbar show/hide toggle** persisted via the settings singleton (`i18n.svelte.ts`, default on); `.editor` became a flex column + `.cm-host` `flex:1;min-height:0` so the added row doesn't overflow the pane. Reviews: **pre-code (197 agents)** — the confirmed BLOCKER (clamp on all transforms) + ~15 recurring transform-correctness rules (UTF-16 offsets, `*`/`**` disambiguation, numbered-regex, mutual exclusivity, setext-H2-safe block insert, layout flex) folded into the brief before coding. **Post-code (56 agents)** confirmed a HIGH — `insertBlock`'s trailing-blank-line was **dead code** (`after` always starts with `\n`/empty), so a Table inserted before a body line absorbed it as a row (verified via marp-core: rows 2→3); fixed (`post` adds `\n` unless a blank already follows) + folded two recurring corruption vectors (line-prefix must skip `---` separators; selection ending at a line boundary grabbed an extra line) and the whole-doc-replace→minimal-diff change. Evidence: svelte-check **0/0**, vitest **132** (md-format 25, parity, + existing), build ✓; backend untouched (**122 tests** stand). **Runtime browser (verify9c):** toolbar renders 19 buttons, no raw i18n keys; Bold click inserts markers via the dispatch path; **the frontmatter clamp holds on transforms AND blocks** (H2/slide-break/bold at caret {0,0} all land after the `---` fence, theme intact); H2 replaces H1 (`# Hello world`→`## Hello world`); the Table-before-body fix re-verified in the RENDER — `Intro sentence.` is a `<p>`, table has **2 rows not 3**, and the persisted markdown has `| 1 | 2 |\n\nIntro sentence.`; toolbar toggle hides/shows; no page h-scroll; dark-mode buttons ~13:1; **zero external requests**.

### 2026-07-25
- **BRIEF-0013 implemented** (Gemini as a third AI provider — direct user request "ต้องการสามารถใช้ API ของ Gemini ในการช่วย Generate สไลด์ได้ด้วย"). `ai.rs` gained a `gemini` arm alongside Anthropic/OpenAI-compatible: `gemini_url()` builds `{base}/v1beta/models/{model}:generateContent` (base trimmed of a trailing `/` so a pasted URL cannot produce `//`), the request body uses `contents[].parts[].text` with `systemInstruction`, and `gemini_text()` concatenates **every** `parts[].text` rather than taking `[0]` (Gemini splits long answers across parts — taking the first silently truncated the deck). **The key travels in the `x-goog-api-key` header, never as a `?key=` query parameter**, so it cannot land in a proxy/access log; provider errors are still never echoed verbatim (they can quote the key back). Admin settings/README/i18n extended with the provider option. Evidence: fmt/clippy clean, full backend suite green, svelte-check 0/0, stack healthy. Committed + pushed as `8acc218`.
- **BRIEF-0014 implemented** (research library + PDF figure extraction — user request "อยากให้ Upload ไฟล์งานวิจัยได้ เพราะสไลด์ของผมจะต้องดึงข้อมูลส่วนใหญ่มาจากงานวิจัย", extended by "ทำให้ระบบ mcp สามารถสกัดภาพจากเอกสารงานวิจัยแล้ว Upload ภาพเพื่อประกอบ Slide"). User decisions taken up front: **PDF + `.txt`/`.md` only**, **server-side text extraction sent as prompt context** (provider-agnostic, not a multimodal upload), **one central per-user library reusable across every deck**.
  Built: migration 0010 (`research_docs` + `research_figures`, children dropped first in the down); `research.rs` (~950 lines) — upload with magic-sniff + 10 MB cap, text extraction (`pdf-extract` for PDF, UTF-8 passthrough for text) inside `spawn_blocking` + `catch_unwind`, list/preview/delete, figure extraction via `lopdf` image XObjects, owner-scoped figure serving on its own session layer under the reserved `/research/` prefix, and attach-to-deck which reuses `assets::store_bytes` so a copied figure is an ordinary deck asset. `ai.rs` gained `research_ids` on the generate request, budgeted by `cap_bytes` and ordered research → existing deck → user prompt. Three MCP tools (`list_research`, `list_research_figures`, `attach_figure`) so an external client can illustrate slides with a paper's own charts; the tool list is now 7. Frontend: `/app/research` page + a **From research** tab in the image picker. New deps: `pdf-extract`, `lopdf`, `flate2` (all offline — extraction adds **no** outbound call; CLAUDE.md §2's two-call rule is unchanged).
  **The hard part was real-world PDFs, not the plumbing.** Figures are JPEG XObjects whose stream is usually wrapped in a transport filter — a real chain is `[/ASCII85Decode, /DCTDecode]`, so `stream.content` is *not* JPEG bytes and a naive magic-byte check silently skipped every figure. Diagnosed by generating a genuine PDF and inspecting it; fixed with `ascii85_decode`/`flate_decode`/`jpeg_payload`, then verified the extracted bytes open as a real 240×180 JPEG.
  **Post-code adversarial review found bugs the earlier passes had missed, all fixed:** (1) **CRITICAL — `delete_doc` removed the parent `research_docs` row before its `research_figures` children under `foreign_keys(true)`**, so deleting any PDF that had figures would fail *forever*; children now go first. (2) **CRITICAL — figure extraction consulted only the inline half of `get_page_resources()`**, but most real PDFs put `/Resources` behind an indirect reference → zero figures on typical papers; both halves are now read. (3) HIGH — synchronous PDF parsing ran on a tokio worker (moved to `spawn_blocking`). (4) HIGH — no XObject dedup, so one repeated logo could eat the 40-figure budget (`HashSet<ObjectId>`). (5) MEDIUM — the prompt budget was a *byte* constant enforced as a *char* count, so Thai research sent ~3× the intended payload (`cap_bytes` on a char boundary). (6) MEDIUM — unbounded `researchIds` per request (clamped).
  Also self-caught: the three new Cargo deps were silently reverted mid-session, breaking the Docker build (`unresolved module flate2/lopdf`) — restored and the lock regenerated in Docker.
  Evidence: `cargo fmt --check` + `cargo clippy --all-targets --locked -- -D warnings` clean; full backend suite green including **research 11/11** and **mcp 11/11**; svelte-check **0 errors / 0 warnings**, vitest 132, `npm run build` ✓; `docker compose -p deckoala up -d --build` → `deckoala-app Up (healthy)`, `/api/health` = `{"status":"ok","version":"0.1.0","db":"ok","chromium":true}`. Tests now include a **real PDF fixture** (`backend/tests/fixtures/paper.pdf`) exercising text extraction, figure extraction, JPEG-decodability of the served bytes, attach-to-deck, and the delete-with-figures FK path — the text-only delete test could never have caught bug (1).
  **NOT evidenced — the authenticated `/app/research` browser walkthrough.** Sign-in was impossible this session: `root`'s password was changed in an earlier session (already noted above) and the implementation tool's operating rules forbid creating an account or entering credentials. Previous briefs were verified with a freshly-registered account, which is no longer permitted. This is a genuine gap against CLAUDE.md Rule 5 ("a server returning 200 is not proof the UI rendered") and must be closed by the user.
- Test-fixture note: `backend/tests/fixtures/paper.pdf` is a generated 7.3 KB paper (text + one embedded JPEG chart) committed deliberately so the PDF path is covered without a network fetch.
- Repo-hygiene fix found while committing: git treated the new `paper.pdf` fixture as **text** and warned it would rewrite its line endings on checkout — a fresh clone on Windows would have smudged CRLF into the PDF and broken figure extraction for everyone but this working copy. Added `.gitattributes` marking `*.pdf/png/jpg/webp/woff2/ttf/...` as `binary`, then **proved it** by cloning the repo to a temp dir and `cmp`-ing the checked-out fixture: byte-identical (7489 bytes).
- Both commits pushed: `4455ae9` (BRIEF-0014) + `d88cb50` (.gitattributes) → `origin/main`.
