# SESSION_STATE — Deckoala

> Read this file FIRST at the start of every session. The Resume Pointer below is the single next action.

## ▶ Resume Pointer
Cowork: audit BRIEF-0010 result (user returns with "เสร็จแล้ว/done"), then write **BRIEF-0011 — MCP server** (`/mcp` over HTTP + per-user revocable API tokens, scoped to that user's decks) so external AI clients can connect. Queued after: BRIEF-0009b (command palette + shortcuts), BRIEF-0009c (theme gallery + per-deck CSS), BRIEF-0012 (visual editor). (User approved all REQ-ANALYSIS §7 nice-to-haves; sequenced one brief at a time.)

---

## 1. Project snapshot
- **What:** Deckoala — self-hosted Markdown/LaTeX presentation web app (live preview, present in browser, PDF export, custom fonts, share links) at `deckoala.dimenshade.com`
- **Who/for:** the user + people they share the instance with (multi-user)
- **Replaces:** ad-hoc slide tools; must be trivially self-hostable (one compose.yml)
- **Language convention:** discussion/analysis docs in Thai; briefs, code, identifiers, UI copy in English (Thai UI i18n on roadmap)

## 2. Roles & workflow
- **Cowork** = design, briefs, audit. **Implementation tool** = code. **Serial only** (see CLAUDE.md §0).
- Loop: Cowork brief → user `ลุย <task>` → impl builds → user "done" → Cowork audit (PASS→next / FAIL→FIX prompt).

## 3. Architecture decisions (summary)
- [ADR-0001](decisions/ADR-0001-stack.md): SvelteKit static SPA + Rust/Axum + SQLite, single container `[USER]`
- [ADR-0002](decisions/ADR-0002-slide-engine.md): Marp Core rendered client-side; deck format = standard Marp Markdown
- [ADR-0003](decisions/ADR-0003-pdf-export.md): PDF via headless Chromium in-container, driven by chromiumoxide
- Full design: [docs/design/ARCHITECTURE.md](docs/design/ARCHITECTURE.md) · Requirements: [REQ-ANALYSIS-v1](docs/requirements/analyzed/REQ-ANALYSIS-v1.md)
- Brand: bg `#F8F8FF`, ink `#0B1215`; logo `assets/brand/logo.svg` (selection log: [docs/design/brand/logo-candidates.md](docs/design/brand/logo-candidates.md))

## 4. Briefs / task queue
| Command | Brief | Status |
|---|---|---|
| `ลุย BRIEF-0000` | infra & scaffolding | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0001` | auth & users | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0002` | deck CRUD + dashboard | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0003` | editor + live preview + autosave + revisions | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0004` | slide rail reorder + image upload | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0005` | present mode + speaker notes + presenter view | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0006` | PDF export via headless Chromium | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0007` | font manager (upload + Google Fonts) | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0008` | sharing (view/edit share links) | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0009` | polish: Thai UI i18n + app dark mode | **done** (2026-07-21, pr-review PASS) |
| `ลุย BRIEF-0010` | admin settings + root bootstrap + AI slide generation | **done** (2026-07-21, pr-review PASS) |
| BRIEF-0011 | MCP server (`/mcp` + per-user tokens) | **next** |
| BRIEF-0009b/0009c/0012 | command palette · theme gallery · visual editor | queued |

## 5. Progress log
### 2026-07-21
- Requirements captured from user brief (Thai) → REQ-ANALYSIS-v1; discussion round 1 closed Q1–Q4 (framework SvelteKit, backend Rust, drag & drop phase-1 simple, multi-user + share links).
- ADR-0001..0003 accepted; ARCHITECTURE.md written; CLAUDE.md + skeleton laid down.
- Logo designed (4 candidates → 3-judge panel → `ears-monogram` won); saved to `assets/brand/` + visual check passed at 16–256px.
- BRIEF-0000 written and registered; adversarial doc-review (3 lenses, 12 findings) applied: scoped port/font invariants, added `db` to health gate, volume chown, `DECKOALA_STATIC_DIR`, reserved prefixes `/api|/assets|/fonts`, roadmap +BRIEF-0010 visual editor, presenter view → BRIEF-0005, PWA/theme gallery moved back to pending-user-choice.
- **BRIEF-0000 implemented** (same session, user said `ลุย`): SvelteKit SPA + Axum server + SQLite migrations (reversible pair) + Docker (multi-stage, chromium, non-root, healthcheck) + compose. Evidence: `cargo test` 2 passed / clippy `-D warnings` / fmt clean (Docker verify stage); `svelte-check` 0 errors; compose up → container healthy; `/api/health` = `{"status":"ok","db":"ok","chromium":true}`; landing verified in browser desktop + 375px mobile; teardown leaves only the volume. Rust pinned 1.88.0 (deps' MSRV forced bump from 1.84; Cargo.lock committed, builds `--locked`).
- Note for next briefs: down-migrations exist from 0001 but executing them needs sqlx-cli — bring it in with BRIEF-0001.
- **BRIEF-0001 implemented** (same session): register/login/logout/me + /api/instance; argon2id (pinned m=19456,t=2,p=1, spawn_blocking); tower-sessions SqliteStore (Lax, 30d, cycle_id on login, hourly expired-session cleanup); origin-check CSRF (authority vs Host or DECKOALA_PUBLIC_URL, null→403); atomic first-user-admin INSERT; login: uniform 401 + dummy-hash timing + 128-byte cap; SPA: /login 3-mode form, guarded /app shell, +error page. Two adversarial review rounds (design 11 findings → brief tightened pre-code; implementation 5 findings → all fixed, incl. Vite changeOrigin:false and logout-error surfacing). Evidence: 19 tests passed (Docker verify), svelte-check 0 errors, compose healthy, curl flow 201→200→204→401/403, browser UI flow desktop + 375px mobile (measured no h-scroll; pane input broke mid-run → clicks dispatched via DOM, same app handlers). New env: DECKOALA_ALLOW_SIGNUP / DECKOALA_SECURE_COOKIE / DECKOALA_PUBLIC_URL.
- Deferred to hardening brief (recorded in BRIEF-0001): rate limiting/lockout, register-path username enumeration.
- **BRIEF-0002 implemented** (same session): migration 0003 decks (+ `foreign_keys(true)` now enforced); AuthUser extractor; CRUD list/create/get/PATCH/soft-delete/duplicate/export with owner-scoping-as-404 on reads AND writes; default Marp template; export Content-Disposition (remove-filter ASCII + RFC5987 filename*); fixed-width `now_rfc3339` (lexicographic ordering); DefaultBodyLimit 4MB; dashboard (grid, new/import/rename/duplicate/export/delete, empty state) + read-only deck page. Two review rounds again (brief: 9 findings pre-code; code: 5 nits — all fixed incl. surrogate-pair-safe filename truncation and 401→/login on cached-layout page loads). Evidence: 37 tests passed (9 unit + 11 auth + 14 decks + 3 health), svelte-check 0/0, compose healthy, curl CRUD flow verified, browser flow desktop+375px incl. import-via-DataTransfer (title from filename), teardown clean earlier this session.
- Incident note: a tool-call edit containing backslash-u unicode escapes got decoded into literal control bytes inside +page.svelte (ripgrep then treated the file as binary) - rewrote the file cleanly; lesson: avoid backslash-u escape sequences in tool-call payloads, prefer codePoint filters in JS.
- **BRIEF-0003 implemented** (same session): migration 0004 revisions; transactional PATCH snapshot (BEGIN IMMEDIATE via sqlx begin_with → auto-rollback on drop, 300s throttle, stale-baseUpdatedAt forces snapshot for the clobber case, retention cap 50 with rowid tie-break); restore/list/get routes (owner+deck scoped, cross-deck revId 404); CSP header (blocks external egress from decks); CodeMirror 6 + marp-core client-side preview in Shadow DOM via **constructable stylesheets** (CSS never HTML-parsed → style-directive XSS breakout impossible), deckoala theme (#F8F8FF/#0B1215 + Noto Sans Thai), KaTeX with locally-bundled fonts (katexFontPath=/katex-fonts/ + document-level katex.min.css import — zero external requests); autosave state machine (debounce, retry, epoch guards, beforeNavigate flush for SPA nav, in-flight-save awaited before restore); revisions panel with view/restore. Two review rounds (brief: 8 findings folded pre-code incl. BEGIN IMMEDIATE, stale-base snapshot, CSP, sizeBytes-as-blob, cross-deck test; code: 6 findings fixed incl. the blocker SPA-nav data loss and the </style> XSS breakout → constructable stylesheets). Evidence: 49 tests passed (Docker verify), svelte-check 0/0, compose healthy, browser: live Thai+KaTeX preview / autosave persists across reload / revision view+restore / CSP blocks external img / XSS probe did not fire / desktop split + mobile tabs.
- launch.json written (.claude/launch.json: compose 8321, vite 5173, cargo 8080) — none started per user (compose stack already up).
- Deviation log (BRIEF-0003): none — KaTeX stayed on the primary (local-font) path; MathJax fallback not needed.
- **BRIEF-0006 implemented** (same session): PDF export via headless Chromium (ADR-0003). Deps added: chromiumoxide 0.7 (default-features=false + tokio-runtime), hmac/sha2/rand. NO migration (stateless HMAC print tokens; in-memory `print_secret`). `POST /api/decks/{id}/export/pdf` (session, owner-check before any Chromium): sha256(markdown+theme) cache under /data/exports (atomic temp+rename write), else acquire semaphore permit (2) → mint token → `render_pdf`: launch Chromium (`--no-sandbox --disable-gpu --disable-dev-shm-usage --host-resolver-rules` loopback-only SSRF enforcement) → CDP set `deckoala_print` cookie → navigate `/print/{id}` on `local_addr` (from DECKOALA_BIND) → poll `window.__DECKOALA_PRINT_READY` → printToPDF (prefer_css_page_size, 0 margins) → stream `application/pdf` with `.pdf` filename (shared `content_disposition(title, ext)` factored to lib.rs). RAII `BrowserGuard` tears the browser down on every path (whole render wrapped in `timeout(30s)`; close-before-abort ordering). `GET /api/print/{id}` (print-cookie only) + `/assets` serve extended to accept a matching print cookie (so Chromium loads deck images) — serve auth unified to session-owner OR print-cookie, unauthorized now 404 (was 401). Frontend: `/print/[id]` (token-fetch, all slides as full-bleed pages sized to viewBox, fonts+images awaited via tick()+decode → ready flag), PDF export buttons on editor + dashboard. Three review rounds (brief: 8 findings folded incl. Chromium-leak-DoS, token-TTL-in-queue, image-readiness, port-from-BIND, SSRF-at-browser-layer; code: 5 fixed incl. non-atomic cache write, BrowserGuard abort-before-close hang/leak, launch-outside-timeout permit pin, tick() vs microtask, safe_segment on export id). Evidence: 70 backend tests (Docker verify, NO Chromium there — token/print-cookie/asset-auth/owner units), vitest 16, svelte-check 0/0, fmt/clippy clean; **real Chromium in runtime container: export → application/pdf, %PDF-, image XObject + fonts embedded (Thai + KaTeX), ~2.8s; cache hit ~16ms identical bytes; owner 404; filename PDF Test.pdf.**
- New env note: DECKOALA_BIND port is honored by PDF export (Chromium dials 127.0.0.1:<that port>).
- **BRIEF-0004 implemented** (same session): migration 0005 assets; multipart upload (`POST /api/decks/{id}/assets`, magic-number sniff PNG/JPEG/GIF/WebP incl. RIFF+WEBP fourcc, 5MB cap, 8MB per-route body limit override verified, owner-scoped 404, safe_segment on deck_id + filename) + owner-scoped serve (`GET /assets/{deck}/{file}` on its own cloned SessionManagerLayer outside /api, nosniff + immutable cache). Frontend: `slides.ts` deriving slide boundaries from Marp's own `marpit_slide_open` tokens (setext/comment/indented-`---` never split → thumbnail↔reorder indices can't desync), CRLF-preserving reorder; slide rail with per-thumbnail shadow roots adopting the shared marpSheet (styled thumbnails, no re-render), native DnD reorder through the editor state path (not setEditorContent); image drop+paste (capture-phase) → upload → insert `![alt](/assets/…)` with markdown-escaped alt. katex CSS import moved to the editor so marp.ts is node-importable by vitest. Three review rounds (brief: 1 blocker [slide-split desync] + 6 fixed pre-code; code: 3 fixed incl. upload-race-on-deck-switch guard + multi-image partial-failure sync + upload deck_id safe_segment). Evidence: 61 backend tests + 16 vitest passed (Docker verify + native), svelte-check 0/0, compose healthy; browser: rail 3 thumbnails render, DnD reorder persists across reload, image drop/paste uploads + renders same-origin, 4.7MB upload proves 8MB override, alt-injection stripped, mobile 375px no h-scroll. New: vitest (`npm run test:unit`) is a native frontend gate.
- Deferred (BRIEF-0004, noted in brief): asset GC/orphan cleanup, storage quota, media library panel, touch-drag reorder on mobile.
- **BRIEF-0005 implemented** (same session, frontend-only — no backend change): `renderDeck` now returns per-slide `notes` from marp `comments` (directive comments like `<!-- _class -->` are auto-excluded, verified). New route `/present/[id]` (own owner-scoped load, 401→login / 404→error): audience mode (one slide letterboxed on ink via the SVG's own viewBox+preserveAspectRatio, keyboard ←/→/Space/PageUp/Dn/Home/End/n/p, `f` fullscreen, touch swipe, idle-auto-hiding control bar) and presenter mode `?presenter` (current + next slide in separate shadow hosts sharing the marpSheet, speaker notes as text, mm:ss timer with pause/reset, prev/next/Exit). Cross-window sync via `BroadcastChannel('deckoala-present-<id>')` with a hello/state handshake (fixes presenter-opens-on-wrong-slide) + hash seed; nav in either window moves both. Present entry points added to editor top bar + dashboard cards. Fixes from 2 review rounds (design: 6 findings incl. 2-surface presenter render, empty-deck guard, handshake, Esc-vs-fullscreen race, aspect-ratio letterbox; code: 5 incl. hash-seed-vs-handshake, `:hover` bar unusable on touch → JS idle timer, focused-button swallowing keyboard nav → blur, presenter Exit button + window.close fallback, popup-blocked notice). Also fixed a render bug found in-browser: extracting a lone `<svg>` dropped the `.marpit` wrapper the theme scopes to → slides rendered transparent; now wrapped (applied to the editor rail too). Evidence: svelte-check 0/0, vitest 16, build ✓, backend suite unchanged (61 tests green from BRIEF-0004 verify); browser: slide renders letterboxed + KaTeX, arrow/swipe nav + counter, presenter shows current/next/note/timer, handshake synced presenter to the audience's slide with NO hash, bidirectional sync, **note shown in presenter only (not on slide)**, mobile swipe no h-scroll.
- **BRIEF-0010 implemented** (same session — NEW user requirement: AI + MCP + admin settings + default admin account): migration 0008 `settings` (key/value). **Root bootstrap**: `auth::seed_root` seeds `root` (is_admin=1) with `DECKOALA_ROOT_PASSWORD` or the built-in `Admin123456@`, called **only from `main.rs`** — deliberately NOT `init_db()`, because every integration test calls `init_db` and relies on "first signup is admin" (seeding there would have broken the whole suite). Idempotent single-statement insert; the `root_password_is_default` flag is set **only** when the built-in default was used, and cleared on change. New `POST /api/auth/password` (verifies current, argon2 reuse, rotates the session; other sessions survive — deferred hardening, tower-sessions has no per-user enumeration). `/api/auth/me` gained `rootPasswordIsDefault` + `aiEnabled` via a separate `MeDto` so register/login shapes are unchanged, and both flags are **session-gated — never on the anonymous `/api/instance`** (tested: key absent, not false). **Admin settings** (`settings.rs`, AdminUser-gated): GET returns provider/baseUrl/model + `apiKeySet`/`apiKeyLast4` but **never the key**; PUT keeps the stored key when omitted, has an explicit `removeApiKey`, and refuses to enable without base URL + model + key. **AI** (`ai.rs`): Anthropic + OpenAI-compatible adapters (local Ollama/LM Studio supported), `join_url` never doubles `/v1`, per-user throttle + global semaphore + prompt/context/response caps + `max_tokens` + timeout + `redirect::Policy::none()`, provider errors never echoed (key-leak safe), fenced output stripped. Frontend: `/app/admin` page (TH/EN, dark-clean) with the default-password warning + change-password + AI form (key field write-only), and an **AI button + dialog in the editor gated by an `aiEnabled` prop set ONLY by the owner route** — never on `/s/[token]`. **CLAUDE.md §2 amended**: exactly two server-side outbound calls (Google Fonts + the admin-configured LLM); viewer pages still zero. Env plumbed through `compose.yml` + `.env.example` + README (compose's `environment:` is an allow-list, so a new var would otherwise be silently dropped; blank is treated as unset). Pre-code adversarial brief-review (56 agents) folded **13 findings**. Evidence: fmt + clippy `-D warnings` clean, **cargo test all green** (lib 25, **admin 9/9**, shares 8, decks 14, auth 11, fonts 4, assets 4, export 4, revisions 11 — 0 failures); svelte-check **0/0**, vitest 22. **Runtime on a wiped volume**: startup WARNs about the default; `root`/`Admin123456@` signs in; anonymous `/api/instance` has no flag; `/me` shows `rootPasswordIsDefault:true, aiEnabled:false`; AI generate → **503** while unconfigured; storing a key never returns it (`••••9876`); change-password → wrong 401 / ok 204 / **flag cleared** / old password 401, new 200; AI button + Thai dialog appear for the owner, **absent on an anonymous share-edit link where a direct `/api/ai/generate` returns 401**; zero external requests on every page.
- **BRIEF-0009 implemented** (same session, frontend-only — no backend change): **Thai UI i18n** (default Thai + remembered EN toggle) + **app dark mode** (remembered, OS-default). i18n: `$lib/messages.ts` = pure catalog (th+en) + `translate()` with a total fallback chain (locale→en→key) and `{name}` interpolation, message values may be functions for pluralization ("1 slide"/"N สไลด์") — kept plain-TS so vitest imports it (no rune-transform in the test config); `$lib/i18n.svelte.ts` = reactive `locale`/`theme` as `$state` on a singleton class (not a reassigned export), `t()`/`formatDate()`/`formatTime()` follow the toggle, all `localStorage`/`matchMedia`/`document` reads `browser`-guarded. `<SettingsToggle>` (lang+theme) in every header; every page/component migrated to `t()` + locale-aware dates. Dark mode: `app.css` promotes the palette to semantic vars (`--dk-surface`, `--dk-danger/success/warn*`) with light `:root` + dark `:root[data-theme=dark]` (AA-adjusted); hardcoded `#fff`/status literals → vars, `color-mix(var(--dk-ink) …)` borders invert for free; **slide surfaces stay brand-light** (thumbnails pinned `#F8F8FF`, preview slides, present stage, /print). `app.html` pre-paint inline script (CSP-safe, no nonce) applies saved lang+theme before first paint (no flash); root `+layout` effect keeps `<html lang>`/`data-theme`/theme-color synced. Two review rounds: **brief-review (30 agents) folded 8** (plain-messages split for vitest, warn/success dark vars + AA, `browser` guards, `$state`-singleton export pattern, parent-injected string localization, pluralization + locale dates, keep CSP unsafe-inline); **code-review** folded (+ 1 self-caught regression: the `#fff`→var sweep darkened slide thumbnails → reverted `.thumb` to `#F8F8FF`). Evidence: svelte-check **0/0**, vitest **22** (slides + new messages suite), build ✓; backend unchanged (cargo suite green from BRIEF-0008). **Runtime:** fresh visitor defaults to Thai; toggle flips every string EN↔TH and back; dark toggle darkens chrome (body `#0f141a`, editor pane `#1b232c`) while the **slide preview + thumbnails measure `#F8F8FF`** (light-branded); both prefs persist across reload/nav; **zero external requests**; Thai renders via bundled Noto Sans Thai. Known limitation (accepted): backend `ApiError` text stays English (frontend-only scope). Nice-to-haves sequenced into follow-up briefs (0009b command palette, 0009c theme gallery).
- **BRIEF-0008 implemented** (same session): migration 0007 `share_links` (`id, deck_id, token UNIQUE, permission view|edit, created_at, expires_at, revoked_at`); `shares.rs` — owner mint/list/revoke (owner-scoped; **revoke double-scoped by id AND deck_id**) + anonymous token routes `GET/PATCH /api/s/{token}` (PATCH edit-only), token revisions list/get/restore (edit), token assets upload (edit), `POST /api/s/{token}/export/pdf` + `GET /api/s/{token}/export` (view+edit). 256-bit hex tokens; unknown/revoked/expired → uniform **404** (no oracle). **Expiry canonicalized to UTC at mint** (RFC3339-with-offset required, naive→422) so the TEXT active-check can't fail open. **Reuse over fork:** decks/export/assets refactored to `pub(crate)` `*_core` fns returning `Response` (never the private rows — dodges `private_interfaces` under `-D warnings`); the revision-snapshot transaction stays single-source (token edit resolves the deck's real owner and delegates). Asset serve now also honours a **per-deck share cookie** (`deckoala_share_{deckId}`, `Path=/assets/{deckId}`, HttpOnly, Secure-from-config), re-resolved to an active share every request → revocation immediate. Anonymous exports use a **separate tighter semaphore (permits=1)** so a leaked edit link can't starve owner exports; `Referrer-Policy: no-referrer` (token is a URL bearer). Frontend: editor extracted to `$lib/components/DeckEditor.svelte` (driven by an `EditorAdapter`), reused by `/app/deck/[id]` (owner) + `/s/[token]` (edit); `/s/[token]` view = read-only render + Present + PDF/.md; `ShareManager` modal (create/copy/revoke + status) on editor + dashboard cards; `SharePresent` audience fullscreen overlay. Two adversarial review rounds: **brief-review (30 agents) folded 4 confirmed** (expiry canonicalization, token-aware present, token .md export, separate export semaphore) + 5 correctness notes; **code-review (token-authz/refactor/frontend)** — token-authz & refactor lenses clean, **1 low frontend finding folded** (SharePresent didn't focus itself → keyboard nav no-op behind the covered Present button; now focuses `.audience` on mount). Evidence: backend fmt/clippy `-D warnings` clean, `cargo test` all green (**shares 8/8** — owner-only mgmt + cross-tenant revoke blocked + view-reads/edit-writes+snapshot + cross-deck 404 + expiry fail-closed + deleted→404 + share-cookie asset auth + migration v6 roundtrip; lib 21), svelte-check **0/0**, vitest 16, build ✓; **runtime, logged OUT (anonymous):** view link renders 3 slides + Thai + image (share cookie), Present, PDF `%PDF-` (2.6s), .md; **view PATCH → 403**; edit link full editor **autosaves via token (revision snapshotted)** + Present (keyboard nav fixed) + PDF, no owner Share control; **revoke → immediate 404 while the other link keeps working**; **zero external requests** on every shared page; owner Share modal shows active/revoked links with copy/revoke.
- **BRIEF-0007 implemented** (same session): migration 0006 `fonts` keyed `(family, weight, style, unicode_range)` so multi-subset Google families (Thai etc.) keep every unicode-range slice; `AdminUser` extractor (reuses AuthUser → 401 without session, then `is_admin` → 403). `fonts.rs`: `upload` (AdminUser, magic-byte sniff wOF2/wOFF/OTTO/ttf, family/weight/style validation, 5MB + 8MB-route caps, 201), `google` (AdminUser, self-built css2 URL from validated inputs, `redirect::Policy::none()`, exact `fonts.gstatic.com` host check via parsed URL, size caps + 10s timeout, ONE transaction), `list` (AuthUser), `delete` (AdminUser, unlinks file only when no other row references it), public `/api/fonts.css` (no-cache, css_escape family) and session-less `/fonts/{filename}` serve (safe_segment, nosniff, immutable) so Chromium/viewers load fonts. Frontend: `/app/fonts` manager (upload + Google install + delete, admin-only) + document-level `<link>` to `/api/fonts.css` in app.html. Deck font picker DESCOPED (frontmatter-writer corruption risk) — users set a font via a Marp `style` directive (page hint shows the snippet). Adversarial 4-lens Workflow review (ssrf/authz/injection/correctness, 14 agents, per-finding verification): **3 findings folded** — (1) `read_capped` treated a mid-stream error as clean EOF → truncated font/css could be stored as "installed"; now returns None on stream error; (2) `google()` left orphan files under /data/fonts when an INSERT hit `ON CONFLICT DO NOTHING` (re-install / add-a-weight); redundant download now deleted, only inserted rows tracked for rollback; (3) defense-in-depth: Google-parsed style/unicode-range clamped/stripped + filename/format css-escaped before reaching fonts.css. Injection/authz findings verified as refuted (server-minted or MITM-only). Evidence: Docker verify green — fmt/clippy `-D warnings` clean, **lib 19 + fonts 4 + all suites passed, exit 0** (incl. migration undo→v5 roundtrip + unicode_range sanitizer unit test); image rebuilt, compose healthy (`/api/health` ok, only 8321 published); **runtime browser: admin upload → 201 in list + fonts.css; Google "Sarabun" [400,700] → 8 subset variants local; browser issued ZERO external requests (all 40 → localhost, no gstatic/googleapis); PDF export subset-embeds `AAAAAA+Sarabun-Bold`/`Sarabun-Regular` (Thai+Latin); orphan-fix confirmed — two full re-installs kept files==rows (9==9), add-weight 600 → 13==13.**

## 6. Open questions / blockers
- Q5 fonts: instance-level admin-managed — shipped that way in BRIEF-0007; no objection raised, considered settled.
- Q6 speaker notes: yes via Marp comments (`[STD]`); presenter view = roadmap.
- Q7 UI language: RESOLVED — Thai i18n shipped in BRIEF-0009, defaulting to Thai with a remembered EN toggle (user's explicit choice).

## 7. Pending tasks for the implementation tool
- None — next step is design-side: Cowork writes BRIEF-0009 (polish: Thai UI i18n + nice-to-haves), then the user hands off with `ลุย BRIEF-0009`.
- Follow-up (deferred from BRIEF-0007): a safe YAML-frontmatter writer so a deck can pick an installed font from the UI (currently done by hand via a Marp `style` directive).
- Follow-ups (deferred from BRIEF-0008, noted in the brief): per-user collaborator invites / real-time multi-cursor; public deck discovery, comments, view analytics; in-place token rotation; email delivery of links.
